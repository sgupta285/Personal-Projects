# OpenAPI Notes

The FastAPI application exposes OpenAPI JSON at `/openapi.json` and interactive docs at `/docs`.

The repository also includes a small Java client example under `java-client/` to demonstrate one way an external team could integrate with the platform.
