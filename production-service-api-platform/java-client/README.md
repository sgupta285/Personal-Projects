# Java Client Example

This small Maven module shows how an integration team could call the platform from Java using the JDK HTTP client. It is intentionally lightweight and avoids code generation so the example stays readable.
