console.log(`
PulseLog ships with seeded demo data.

Run:
  npm install
  npm run start

Open the Expo app on a simulator or device.
Use the Settings tab to force a sync to the mock remote adapter.
`);
